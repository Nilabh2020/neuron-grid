'use client';

import React from 'react';
import Dashboard from '../page';

export default function NodesPage() {
  return <Dashboard />; // For now, we reuse the hardware dashboard for the nodes view
}
